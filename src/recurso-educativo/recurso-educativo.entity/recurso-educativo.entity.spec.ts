import { RecursoEducativoEntity } from './recurso-educativo.entity';

describe('RecursoEducativoEntity', () => {
  it('should be defined', () => {
    expect(new RecursoEducativoEntity()).toBeDefined();
  });
});
