import { TemaEducativoEntity } from './tema-educativo.entity';

describe('TemaEducativoEntity', () => {
  it('should be defined', () => {
    expect(new TemaEducativoEntity()).toBeDefined();
  });
});
