import { TransferenciaEntity } from './transferencia.entity';

describe('TransferenciaEntity', () => {
  it('should be defined', () => {
    expect(new TransferenciaEntity()).toBeDefined();
  });
});
